package tutorapplication.model;

public class Tutor extends User {
    public Tutor(String email, String password, String name, String surname, String role) {
        super(email, password, name, surname, role);
    }
}
